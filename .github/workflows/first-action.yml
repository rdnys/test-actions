name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: macos-latest
    steps:
      - name: Print greeting
        run: echo "Hello world"
      - name: Pring goodbye
        run: echo "Goodbye"
